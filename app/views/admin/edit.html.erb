<h2>Editar usuario <%= @user.name %></h2>

<%= form_for(@user, url: update_user_path) do |form| %>
  <div class="field">
    <%= form.label :admin %>
    <%= form.check_box :admin %>
  </div>

  <div class="actions">
  <%= form.submit %>
  </div>
<% end %>